# UX baseline (WI-0.1)

Measured on commit `1ff008203`, branch `ux/build-20260921`, before any product file changed. No real course and no person: D11 lets an agent do this with the harness and the product text alone (`docs/implementation/ux/MORROW-UX-BUILD-SPEC.md`, Fact D11). Every row below carries its own source citation, marked one of two ways per WI-0.1's instructions:

- **counted from the text.** I read the exact strings and control lists in the source file named, and counted each named control as one action. No script ran.
- **measured in the harness.** A script produced the number directly.

Two numbers in this file are neither: they are the reference document's own earlier estimate, carried forward because I did not independently re-derive them this pass. Each is flagged in place, not presented as a fresh measurement.

## Interaction budget, one row for each moment in `docs/implementation/ux/reference/audit-page.html#budget`

| Moment | Cost today | Target (unchanged from the reference) | How counted |
|---|---|---|---|
| **Setup, to the first read of a course** | About 19 actions with a store listing. About 21 with Developer mode. | 9 or fewer. "Add to Chrome" opens the listing. One sign-in tab is sufficient. | Counted from the text. Walked the panel sequence in `installer/shared/setup-view.mjs`: choose an assistant and set it up (`actionPanel`, :274-281, 2 actions); the Bridge panel, either "Install Morrow Bridge" (:325-330, 3 named controls in the store `<ol>` + 1 "Check Bridge" = 8 actions) or "Add Morrow Bridge" (:318-323, Developer mode `<ol>` + "Check Bridge" = 10 actions); "Open your course in Chrome" (:340-345, 8 named controls); "Check your course connection" (:355-360, 1 action). Store total 2+8+8+1=19. Developer total 2+10+8+1=21. |
| **One approved change** | 2 app switches (assistant to browser for the review, browser back to the assistant), find the link in the assistant's reply, 1 click to approve, 1 typed message to resume the assistant. | 1 click on a link named after the change, 1 click to approve. No typed message. The Bridge popup also lists the review. | Counted from the text. Spec facts F16 (`packages/gateway-core/src/result.ts:47`, the exact resume sentence the assistant receives) and F17 (`packages/mcp-server/src/operation-tools.ts:44`, `approval-server.ts:1229`: no tool waits for the review, so the person must return to the assistant and say to continue). |
| **The same kind of change again, same course, same day** | The full cost again. | 0. The assistant reports it. | Counted from the text. No "do not ask again" memory exists in `src/edit-policy.js` today; D2b/D3 in the spec's decisions table name this as new work (WI-4.x), not yet built. |
| **Ten changes of one kind** | 1 review if the assistant's tool call already batches the changes into one operation; 10 if it issues ten separate operations. | 1 review. Morrow tells the assistant to batch. | Counted from the text. `approval-server.ts` reviews one operation per `operationGet`/`approveOperation` call; nothing in `runtime.ts` or `morrow-runtime.ts` merges separate same-kind operations server-side. WI-2.3 (batching guidance) is unbuilt. |
| **Come back the next day, tabs closed** | Open the platform by hand, open a course, reload the Bridge page. | 0 clicks. The Bridge opens the site when work arrives. | Counted from the text, partial. `src/service-worker.js` has no path that opens a tab when an operation needs a closed course site (D1a's auto-open is new work, WI-1.2). The reference audit page's specific figure "0 of 3 courses were ready on this computer" came from an earlier real-course walkthrough; I did not repeat that measurement this pass because opening a real Canvas, Moodle or Blackboard site is forbidden for this agent, so I report the structural cost only, not a fabricated course count. **Person check, not blocking**, for the course-count figure specifically. |
| **"Can Morrow change anything now?"** | Open Settings and read each course card. | A glance at the Bridge icon. | Counted from the text. No `chrome.action.setBadgeText` (or similar) call exists anywhere in `connector/extension/src/service-worker.js`; the toolbar badge (WI-1.3) is unbuilt. |
| **Stop all access** | Select courses (per-row checkboxes, or "Select this page" for up to 6), then click "Return selected courses to Plan". | 1 click. | Counted from the text. `connector/extension/settings/settings.html:142` (the button, disabled until a selection exists) and `settings.js:1142` (`returnToPlan`), `:1464` (its click handler). The click itself already matches the target; selecting the courses first still costs one action per course today, since there is no single "every course, every page" selector. |
| **"What did Morrow change today?"** | Ask the assistant. No view exists. | 1 click to a list with links to each item. | Counted from the text. No history or "recent changes" list appears in `popup/popup.js` or `settings/settings.js`. `approval-server.ts`'s `operationList` returns a page of operations but is not surfaced as a browsable list in the Bridge UI. WI-6.4 ("Recent changes") is Phase 6, unbuilt. |
| **Find one course among 40** | 7 pages of 6. Search matches the course's provider, origin, site URL, internal course ID, name or principal ID, not a human course code. | Type the course code. | Measured in the harness for the page size: `PAGE_SIZE = 6` at `connector/extension/settings/settings.js:62`, so 40 courses paginate into `⌈40/6⌉ = 7` pages. Counted from the text for the search fields: `matchingItems()` at `settings.js:358-365` filters on `provider, origin, siteUrl, courseId, courseName, principalId`; no `courseCode`/`course_code` field is read into a binding anywhere in `settings.js` or `service-worker.js`, so a Canvas course code (for example "BIO101") is not itself searchable, only the internal `courseId` and the display `courseName` are. |
| **Allow a whole area of work** | Up to 141 checkboxes, one at a time. | 1 switch for routine work, or 1 checkbox for an area. | Not independently re-verified this pass; carried from the reference audit page's own earlier count. I confirmed the shape that makes it plausible: `settings.js:557` renders one `<input type="checkbox">` per category with no group-level toggle beside it, and spec fact F10 gives the catalog scale behind that count (347 grantable Canvas options, 58 of which even a full grant changes nothing in, at `src/edit-policy.js:26`, `:496`, `:526`). I did not re-walk the curated category tree to recompute 141 directly. |

## Census table (WI-T.1, `node scripts/ux/census.mjs`, this commit)

Reads computed styles only; changes nothing. WI-F.2's targets: 7 sizes, 3 weights, 4 line heights, 4 radii, small text and small targets both 0, no long lines, gaps and paddings on the space-token set. None of the five surfaces meet the full target set yet, which is expected before Foundation (RF) ships.

| Surface | Sizes | Weights | LineHt | Radii | Gaps | Paddings | Small text | Small targets | Long lines |
|---|---|---|---|---|---|---|---|---|---|
| Popup | 4 | 4 | 4 | 4 | 3 | 7 | 21 of 27 | 2 | 0 |
| Settings page | 8 | 5 | 9 | 6 | 8 | 11 | 6 of 36 | 3 | 12 |
| Setup guide | 5 | 4 | 6 | 4 | 3 | 6 | 0 of 21 | 1 | 10 |
| Edit list | 8 | 4 | 7 | 2 | 3 | 9 | 5 of 14 | 1 | 4 |
| Desktop app, set up | 9 | 8 | 10 | 7 | 12 | 15 | 26 of 50 | 0 | 10 |

Command: `node scripts/ux/census.mjs`, run from the worktree root on commit `1ff008203`. The table above is that run's output, transcribed unedited.

## Batch B

Batch B is WI-T.1 (the census tool) and WI-0.1 (this document). No product file changed in this batch, so no budget row has an old value to compare against; every row above is this batch's own first measurement, not a change from a prior one.

Re-ran `node scripts/ux/census.mjs` after the batch's files were in place, from the worktree root, no product file touched. Output reproduced the census table above exactly, cell for cell:

| Surface | Sizes | Weights | LineHt | Radii | Gaps | Paddings | Small text | Small targets | Long lines |
|---|---|---|---|---|---|---|---|---|---|
| Popup | 4 | 4 | 4 | 4 | 3 | 7 | 21 of 27 | 2 | 0 |
| Settings page | 8 | 5 | 9 | 6 | 8 | 11 | 6 of 36 | 3 | 12 |
| Setup guide | 5 | 4 | 6 | 4 | 3 | 6 | 0 of 21 | 1 | 10 |
| Edit list | 8 | 4 | 7 | 2 | 3 | 9 | 5 of 14 | 1 | 4 |
| Desktop app, set up | 9 | 8 | 10 | 7 | 12 | 15 | 26 of 50 | 0 | 10 |

Measured in the harness.

Budget rows this batch changed, one line per row in the "Interaction budget" table above, each carrying the same source mark the row itself carries:

- Setup, to the first read of a course: measured in the harness for the census table's font/gap/target counts, cited above. Counted from the text for the 19/21-action walk through `installer/shared/setup-view.mjs`, unchanged by this batch.
- One approved change: counted from the text. No harness figure applies; the row cites spec facts F16/F17, unchanged by this batch.
- The same kind of change again, same course, same day: counted from the text. No `src/edit-policy.js` memory exists yet; this batch touched no product file, so the fact stands unchanged.
- Ten changes of one kind: counted from the text. `approval-server.ts` batching behavior unchanged by this batch.
- Come back the next day, tabs closed: counted from the text, partial (see the row's person-check note). `src/service-worker.js` unchanged by this batch.
- "Can Morrow change anything now?": counted from the text. No badge call exists in `connector/extension/src/service-worker.js`; unchanged by this batch.
- Stop all access: counted from the text. `settings.html`/`settings.js` selection flow unchanged by this batch.
- "What did Morrow change today?": counted from the text. No history view in `popup/popup.js` or `settings/settings.js`; unchanged by this batch.
- Find one course among 40: measured in the harness for `PAGE_SIZE = 6`; counted from the text for the searchable-field list. Neither source changed, since this batch touched no product file.
- Allow a whole area of work: carried forward, not independently re-verified this pass, same as the row states. Unchanged by this batch.

Every row's underlying source file is one this batch did not touch (`scripts/ux/**` and `docs/implementation/ux/UX-BASELINE.md` are the only files batch B owns), so every row's value from WI-0.1's original pass stands as this batch's value. Verify command run: `node scripts/ux/census.mjs && node --test scripts/test/no-em-dash.test.mjs`. Last lines of `node --test scripts/test/no-em-dash.test.mjs` and the census run are in the work item report for this batch.

## Batch S1

Batch S1 is the CSS-foundation cards (WI-F.1, WI-F.3, WI-F.4, WI-F.2, WI-F.5, WI-F.6, WI-F.7, WI-F.11), the review-page RF cards (WI-F.8, WI-F.9a), the PAGES RF card (WI-F.10), the "Open Canvas/Moodle" and auto-open cards (WI-1.1a, WI-1.2, WI-1.1b), the toolbar badge (WI-1.3), the banner and remaining PAGES copy cards (WI-1.4, WI-1.5a, WI-1.6a), the desktop setup-screen card (WI-1.5b), and the runtime edit-access card (WI-1.6b). This measurement pass is read-only: no product file was edited for this batch's baseline entry, only `docs/implementation/ux/UX-BASELINE.md`. Measured against the worktree's current state (uncommitted, on top of the batch B checkpoint commit `09dd29b24`), before the batch's own checkpoint commit.

Ran `node scripts/ux/census.mjs` from the worktree root. Output, transcribed unedited:

| Surface | Sizes | Weights | LineHt | Radii | Gaps | Paddings | Small text | Small targets | Long lines |
|---|---|---|---|---|---|---|---|---|---|
| Popup | 2 | 3 | 3 | 3 | 3 | 7 | 0 of 28 | 1 | 0 |
| Settings page | 6 | 3 | 4 | 3 | 6 | 8 | 0 of 36 | 2 | 0 |
| Setup guide | 5 | 3 | 3 | 3 | 2 | 6 | 0 of 21 | 1 | 0 |
| Edit list | 6 | 3 | 4 | 2 | 3 | 9 | 0 of 14 | 0 | 1 |
| Desktop app, set up | 5 | 3 | 4 | 4 | 6 | 12 | 0 of 50 | 0 | 4 |

Measured in the harness. Every surface's small-text count is now 0 (font sizes are up off the 13px floor across the board), and four of five surfaces already sit at or under WI-F.2's weight and line-height targets (3 weights, 4 or fewer line heights); Settings page and Desktop app still carry more distinct sizes (6, 5) than the 7-size target allows headroom for once combined with their gap/padding counts, and neither is at `--check` green yet. This is expected mid-batch: WI-F.2 through WI-F.7 land the token moves in sequence, and WI-F.10/1.1b/1.4/1.5a/1.6a add markup after the tokens are in.

Budget rows this batch changed, one line per row in the "Interaction budget" table above, each carrying the same source mark the row itself carries. Old value is the number from WI-0.1's first pass (baseline section above); new value is this batch's:

- **Setup, to the first read of a course.** Counted from the text: unchanged. `installer/shared/setup-view.mjs`, the file the 19/21-action walk-through cites, is not in this batch's file list and is untouched in the working tree; the panel sequence and its action count stand. Measured in the harness: the "Desktop app, set up" surface (which renders that panel through `installer/renderer/renderer.js`, itself in this batch under WI-1.5b) changed. old Sizes 9 / Weights 8 / LineHt 10 / Radii 7 / Gaps 12 / Paddings 15 / Small text 26 of 50 / Small targets 0 / Long lines 10, new Sizes 5 / Weights 3 / LineHt 4 / Radii 4 / Gaps 6 / Paddings 12 / Small text 0 of 50 / Small targets 0 / Long lines 4, from the CSS-foundation cards plus `installer/renderer/styles.css`. The action count itself is a separate fact from these style counts and did not move.
- **One approved change.** Counted from the text: unchanged. `packages/gateway-core/src/result.ts` (F16) is untouched in the working tree, and the diff to `packages/mcp-server/src/approval-server.ts` (43 insertions / 6 deletions, checked against `git diff HEAD`) touches only rendering functions (`changeTitle`, `keepOpenInstruction`, `snapshotPlatform`, `statusContent`, `html`), not `operationGet`/`approveOperation` or the "no tool waits" behavior F17 cites; the person must still return to the assistant and say to continue. Measured in the harness: the "Edit list" surface (the review page WI-F.8/WI-F.9a restructure, moving structural fields into Technical details) changed. old Sizes 8 / Weights 4 / LineHt 7 / Radii 2 / Gaps 3 / Paddings 9 / Small text 5 of 14 / Small targets 1 / Long lines 4, new Sizes 6 / Weights 3 / LineHt 4 / Radii 2 / Gaps 3 / Paddings 9 / Small text 0 of 14 / Small targets 0 / Long lines 1.
- **The same kind of change again, same course, same day.** Counted from the text: unchanged. `src/edit-policy.js` is not in this batch's file list and is untouched; no "do not ask again" memory exists yet (that is WI-4.x, not in S1).
- **Ten changes of one kind.** Counted from the text: unchanged. WI-2.3 (batching guidance) is not a card in this batch. `packages/mcp-server/src/approval-server.ts` and `packages/mcp-server/src/morrow-runtime.ts` already carry a `batches` kind and a `BatchWindowScheduler` (from `@morrow/batch-engine`) in the base commit, but that machinery groups the items of one already-batched tool call for display; nothing in this batch adds server-side merging of separately issued same-kind operations into one review, so the row's fact ("1 review if the assistant's tool call already batches the changes; 10 if it issues ten separate operations") stands.
- **Come back the next day, tabs closed.** Counted from the text: **changed.** Old: "`src/service-worker.js` has no path that opens a tab when an operation needs a closed course site (D1a's auto-open is new work, WI-1.2)." New: WI-1.2 is in this batch and is built. `bindingForCommand()` (`connector/extension/src/service-worker.js:5990`) calls `openPlatform()` once, with no consent step, when a command's binding is not runtime-verified, names a saved site, and matches the operation's provider, gated by the `openPlatformWhenNeeded` storage key (default on; a missing key reads as on, confirmed at `:819` and `:5995`). It retries the binding read once after the open, and records an `openPlatformNotice` for the popup's one-line "Morrow Bridge opened Canvas to continue your request." (P2). New `scripts/test/open-platform.test.mjs` (untracked, added this batch) covers the setting-on/one-tab/one-retry, setting-off/no-tab, and no-tab-when-one-exists cases per the spec's WI-1.2 acceptance. This is a genuine "0 clicks" improvement over the old "open the platform by hand" cost, for the case the setting covers; it does not cover a course with no saved site at all, which still throws `platform_open_anchor_missing`.
- **"Can Morrow change anything now?"** Counted from the text: **changed.** Old: "No `chrome.action.setBadgeText` (or similar) call exists anywhere in `connector/extension/src/service-worker.js`." New: WI-1.3 is in this batch and is built. `refreshBadge()` (`:1611`) sets the badge to the review count when `state.reviewsWaiting > 0`, else to "ON" (with the action color) when one or more connections has a valid, unexpired Edit permission (computed for real from `stored.bindings`, `storedPolicies`, and `validEditPermission`, not stubbed), else clears it; it runs at the end of `publishBindings()`, after a policy save or revoke, on the `ui_state` command, and on a `chrome.alarms` alarm at the soonest permission expiry (`BADGE_ALARM_NAME`, wired at `:6149`). `state.reviewsWaiting` itself is still 0 in this batch, because WI-2.4 (R2) is not a card here, so the badge currently only ever shows "ON" or empty, never a review count, until R2 lands; the "glance at the icon" cost for the Edit-permission case is real today. New `scripts/test/toolbar-badge.test.mjs` (untracked, added this batch) covers the three states and the expiry alarm.
- **Stop all access.** Counted from the text: unchanged. `connector/extension/settings/settings.html` and `settings.js` are in this batch's file list (WI-F.10, WI-1.1b, WI-1.4, WI-1.5a, WI-1.6a all touch them), and the cited line numbers moved: the button is now `settings.html:146` (`id="return-plan"`, still disabled until a selection exists), `returnToPlan` is now `settings.js:1233`, and its click handler is now `settings.js:1582`. The selection-then-one-click shape is unchanged; no "every course, every page" selector was added.
- **"What did Morrow change today?"** Counted from the text: unchanged. No history or "recent changes" list was added to `popup/popup.js` or `settings/settings.js` in this batch (WI-6.4 is Phase 6, not in S1).
- **Find one course among 40.** Measured in the harness: unchanged. `PAGE_SIZE = 6` still, now at `settings.js:65` (moved from `:62`); `⌈40/6⌉ = 7` pages stands. Counted from the text: unchanged. `matchingItems()` (`settings.js:421`) still filters on `provider, origin, siteUrl, courseId, courseName, principalId`; no course-code field was added in this batch.
- **Allow a whole area of work.** Not independently re-verified this pass, same status as the baseline row. `src/edit-policy.js` is untouched in the working tree, so the 141-checkbox, F10-catalog-scale fact this row carries is unchanged by this batch; I did not re-walk the category tree in `settings.js`.

Verify command run for this measurement: `node scripts/ux/census.mjs`, from the worktree root, no product file touched by this work item. `packages/mcp-server/dist/` was confirmed newer than `packages/mcp-server/src/` for the two changed source files (`approval-server.ts`, `edit-access.ts`) before running, so the census reads this batch's built output, not a stale build. `df -g /Users/Braden` reported 394 GB free before the run.

## Batch S2

Batch S2 is the Phase 2 review-loop cards (WI-2.1, WI-2.2, WI-2.3, WI-2.4a to WI-2.4e, WI-2.5, WI-2.6) and the Phase 3 words-and-task-bundles cards (WI-3.5a, WI-3.1a to WI-3.1c, WI-3.2, WI-3.3, WI-3.4, WI-3.5b, WI-3.5c). This measurement pass is read-only: I edited no product file, only this document. Measured against the worktree's current state (uncommitted changes on top of the batch's own first checkpoint commit `a86efcd62`, "S2 part 1 review loop, ui_state, bundles, plain labels, cross-lane repairs"), before the batch's closing checkpoint commit. `git diff --stat 36d3b59ec` (the S1 checkpoint this batch started from) against the current working tree touches 51 files, all inside the S2 cards' file lists; no file under `installer/` is among them, and no lane outside PROTOCOL, RUNTIME, SW, PAGES, REVIEW, CATALOG or POLICY is touched.

`df -g /Users/Braden` reported 396 GB free before `pnpm build` and again before the census run. Ran `pnpm build` from the worktree root: all 13 buildable packages finished `Done`, so this pass rebuilt `bridge-protocol`, `gateway-core`, `bridge-loopback`, `canvas-connector-mcp`, `canvas-api-catalog` and `mcp-server` (the six packages this batch's diff touches) before measuring, and the census reads this batch's built output, not stale `dist`.

Ran `node scripts/ux/census.mjs` from the worktree root, three times in a row for reproducibility (identical output each run). Output, transcribed unedited:

| Surface | Sizes | Weights | LineHt | Radii | Gaps | Paddings | Small text | Small targets | Long lines |
|---|---|---|---|---|---|---|---|---|---|
| Popup | 2 | 3 | 3 | 3 | 3 | 7 | 0 of 27 | 1 | 0 |
| Settings page | 6 | 3 | 4 | 3 | 6 | 8 | 0 of 36 | 2 | 0 |
| Setup guide | 5 | 3 | 3 | 3 | 2 | 6 | 0 of 21 | 1 | 0 |
| Edit list | 6 | 3 | 4 | 2 | 3 | 9 | 0 of 14 | 0 | 1 |
| Desktop app, set up | 5 | 3 | 5 | 4 | 6 | 12 | 0 of 50 | 0 | 4 |

Measured in the harness. Four of five surfaces (Popup, Settings page, Setup guide, Edit list) match the Batch S1 table in this document cell for cell, consistent with S2 touching none of their owning files for style purposes. Two cells differ from the S1 table, and I traced both before writing this section, because neither owning file is in an S2 card:

- **Desktop app, set up: LineHt 4 in the S1 table, 5 now.** `installer/renderer/styles.css` is untouched by S2 (`git diff --stat 36d3b59ec` above confirms no `installer/` file changed in this batch). I checked the S1 batch's own two checkpoint commits instead: `git diff c69230d09 36d3b59ec -- installer/renderer/styles.css` shows one line changed, `font: 400 1rem/1.45 var(--font-sans);` to `font: var(--text-body);`, in the "S1 review repairs" commit. That commit landed after the S1 section's census table was written into this document (the S1 section states it was measured "before the batch's own checkpoint commit," i.e., before `c69230d09`). So the S1 table's Desktop row was stale relative to S1's own final state by one token swap; this batch did not cause the change and did not touch the file it is in.
- **Popup: "0 of 28" in the S1 table, "0 of 27" now.** Same shape: `git diff --stat c69230d09 36d3b59ec -- connector/extension/popup/` shows `popup-view.js`, `popup.html` and `popup.js` all changed in the same "S1 review repairs" commit, after the S1 table's text was recorded. This batch (S2) also changed all three files again, for WI-2.4e (the popup's new "Waiting for your review" part, `popup.html:22` to `:24`, `id="reviews-waiting"`). I did not isolate how much of the one-element denominator shift traces to the S1 repair commit versus this batch's own WI-2.4e markup, because the new section is empty and `hidden` in the census's stub state (no review is pending for the popup's stub connection), so it contributes 0 visible text elements either way; the small-text numerator (0) and the target's own count (1, unchanged) are both accounted for.

## Interaction budget, this batch

Budget rows this batch changed, one line per row in the "Interaction budget" table near the top of this document, each carrying the same source mark the row itself carries. Old value is the number from the Batch S1 section above (S2's own starting point); new value is this batch's.

- **One approved change.** Counted from the text: **changed, on both halves of the row's fact.** Old: "2 app switches (assistant to browser for the review, browser back to the assistant), find the link in the assistant's reply, 1 click to approve, 1 typed message to resume the assistant," citing F16 (`packages/gateway-core/src/result.ts:47`, the exact resume sentence) and F17 (no tool waits, so the person must return to the assistant and say to continue). New, F16: `result.ts:47`'s `awaiting_approval` sentence is now "Morrow prepared this change. It waits for the person's review. Give the person the one link in receipts.approvalUrl, named after the change. Then call morrow_operation_wait with this operationId. Do not ask the person to type anything." (`packages/gateway-core/test/result.test.ts` updated to match, per WI-2.1's instruction.) `packages/mcp-server/src/runtime.ts:3073` (`effectResult`) now adds an `attention` entry, "Review and approve: <plain label> in <course name>," for the named-link half of the target. New, F17: `native-tool-manifest.ts:66` and `operation-tools.ts:101` add `morrow_operation_wait`, a polling tool (`packages/mcp-server/src/operation-tools.ts`, `WAIT_CONTINUE_STATES`, 500 ms poll, 1 to 50 second cap) that returns as soon as the record leaves `awaiting_approval`/`approved`/`dispatching`/`running`; the assistant calls this instead of the person typing a resume message. `packages/mcp-server/src/review-loop-guidance.ts` (new file, resource `morrow://guidance/review-loop-v1`) tells the assistant to do exactly this. Separately, `connector/extension/popup/popup.html:22` to `:24` (`id="reviews-waiting"`, "Waiting for your review") and `connector/extension/popup/popup-view.js:18` to `:27` (`pendingReviews`, `reviewButtonLabel`) now list any waiting review in the Bridge popup itself, wired from `connector/extension/src/service-worker.js:2129` (`applyBridgeUiState`), so the person can also find the review without the assistant's link at all. I did not attempt the "person check" for the 2-app-switch, 1-typed-message figure itself (this batch built the mechanism; a real walkthrough with an assistant is a person check per the brief), but the structural fact behind the old number (no waiting tool existed) is now false, and the new target's three named pieces (a link named after the change, one click to approve, no typed message, the popup listing it) each have working code behind them as of this batch.
- **The same kind of change again, same course, same day.** Counted from the text: **unchanged in effect, with new groundwork.** Old: no "do not ask again" memory exists; D2b/D3 named as new work, not yet built. New: WI-3.1 and WI-3.2 (this batch) add `rememberable: true` and `routine: true` fields to `src/edit-policy.js`'s curated bundle specs (`CURATED_CATEGORY_SPECS`) and to each generated option (`operationSpec`), and `canvas-bundles.draft.json` marks 6 Canvas bundles plus `canvas_alt_text` as routine, and `canvas_dates` as rememberable-only. This is the data-model half of D2b. The behavior half, an actual "remember this kind of change" offer and grant (`rememberOffer`/`rememberKind` on the runtime, the review page's "do not ask again" control), is WI-4.1 to WI-4.3, Phase 4, and is not a card in S2. So a person still pays the full cost again today; only the field a future phase will read is now present.
- **Ten changes of one kind.** Counted from the text: **changed, the guidance half only.** Old: "1 review if the assistant's tool call already batches the changes into one operation; 10 if it issues ten separate operations," with WI-2.3 (batching guidance) named as unbuilt. New: WI-2.3 is in this batch. `packages/mcp-server/src/server-instructions.ts:5` now reads "After a review link, call morrow_operation_wait. For several changes of one kind, use stage_writes." (replacing a shorter present sentence, per the spec's "make room" instruction, to hold the length under the 4,000-character test in `server-instructions.test.ts`); measured the joined instruction string directly (not the test's own count) at 3,835 characters before this batch's edit, on the S1 checkpoint commit `36d3b59ec`, and 3,900 characters now, both under the 4,000 limit. `review-loop-guidance.ts`'s new resource repeats the instruction in full: "freeze them as one batch with `stage_writes` and give the person one review link for the whole batch, not one link for each change." I found no change to `approval-server.ts`'s one-operation-per-review shape or to `runtime.ts`/`morrow-runtime.ts`'s merge behavior beyond what the S1 section already recorded (the existing `BatchWindowScheduler` groups one already-batched call for display; nothing new in this batch merges separately issued operations server-side). So the mechanical fact stands (1 review only if the assistant already batched the call), but the assistant is now told to do that, where before this batch it was not told anything either way.
- **"Can Morrow change anything now?"** Counted from the text: **changed.** Old: "`state.reviewsWaiting` itself is still 0 in this batch, because WI-2.4 (R2) is not a card here, so the badge currently only ever shows 'ON' or empty, never a review count." New: WI-2.4 is in this batch (cards WI-2.4a to WI-2.4e). `connector/extension/src/service-worker.js:2129` to `:2132` (`applyBridgeUiState`) now sets `state.reviewsWaiting = uiState.reviews.length` from the runtime's pushed `ui_state` command and calls `refreshBadge()`. `refreshBadge()` (`:1611`) shows the numeric count (capped at 99) with a distinct badge color and an accessible title ("Morrow Bridge. N reviews wait.") whenever `reviewsWaiting > 0`, falling back to the existing "ON"/empty Edit-permission logic only when it is 0. The runtime side that feeds this (`packages/mcp-server/src/runtime.ts`, sending the review list after an operation or batch for a browser course enters or leaves `awaiting_approval`) and the popup's own "Waiting for your review" list (above) both draw on the same `ui_state` push. New `scripts/test/ui-state-command.test.mjs` and `scripts/test/toolbar-badge.test.mjs` (both updated in this batch per WI-2.4d's file list) cover the states. The "glance at the icon" target is now met for the review-count case; the earlier Edit-permission "ON" case (built in S1) is unchanged.
- **Allow a whole area of work.** Counted from the text and measured in the harness: **the F10 half changed; the 141-checkbox figure was not independently re-verified this pass and its source citation is now stale.** Old: "Up to 141 checkboxes, one at a time," carried forward from the reference page, with F10 cited as unconfirmed for this project (347 grantable Canvas options, 58 of them `requiresFieldSelection: true` and so grant nothing on their own, at `src/edit-policy.js:26`, `:496`, `:526`). New, measured in the harness: I ran `categoriesForBinding({ provider: "canvas" }, canvasOperations)` (the same call `scripts/test/edit-option-facts.test.mjs` uses) against this batch's built `src/edit-policy.js` and the regenerated `connector/extension/generated/canvas-api-catalog.json` (WI-3.5a ran `pnpm catalog:canvas` in this batch). Result: 338 edit-availability Canvas options today, not 347; 58 of them `requiresFieldSelection`, the same count F10 states. The 9-option gap from 347 to 338 is not something I traced to a specific removed operation; it sits somewhere in this batch's catalog regeneration (WI-3.5a) or an earlier batch's, and F10's "347" in `MORROW-UX-BUILD-SPEC.md:31` is a decisions-table fact, not a value this pass owns or corrects. New, counted from the text: WI-3.4 ("Be honest about options that grant nothing") is in this batch and is built. `connector/extension/settings/settings.js:554` to `:556` (`rebuildCategories`) now excludes every `requiresFieldSelection` option from the selectable set outright (a checkbox for one is never rendered as active), and `settings.js:670` to `:671` shows, for each such option, "Morrow can change this only through a bundle: <bundle labels>." when a routine or rememberable bundle covers it, else "Morrow always asks before this change." This does not lower the 141-checkbox figure itself (I did not re-walk the category tree to recompute it, same as the S1 and baseline entries), but it removes 58 of the options that figure may have included as if they were a live "allow this" action, replacing each with a one-line explanation instead of a dead checkbox.

I did not find a source-file match for the remaining budget rows in this batch's 51-file diff (`git diff --stat 36d3b59ec`), so I recorded each as unchanged rather than omitting it:

- **Setup, to the first read of a course.** Unchanged. `installer/shared/setup-view.mjs` is untouched (confirmed above: no `installer/` file appears in this batch's diff).
- **Come back the next day, tabs closed.** Unchanged. `bindingForCommand()` (now at `connector/extension/src/service-worker.js:6066`, moved from `:5990` by this batch's earlier insertions) and the `openPlatformWhenNeeded` gate (`:6070`) are present but not edited by this batch's diff; SW-lane changes in S2 are the `ui_state` command handler and its dispatch hookup only (`git diff 36d3b59ec -- connector/extension/src/service-worker.js` shows three hunks, none inside `bindingForCommand`).
- **Stop all access.** Unchanged. `connector/extension/settings/settings.html:146` still names `id="return-plan"`, disabled until a selection exists; `returnToPlan` moved to `settings.js:1310` (from `:1233`) as other code was inserted above it, but its body and the selection-then-one-click shape are not in this batch's diff hunks.
- **"What did Morrow change today?"** Unchanged. No history or "recent changes" list was added to `popup/popup.js` or `settings/settings.js` in this batch; WI-6.4 is Phase 6, not a card in S2.
- **Find one course among 40.** Unchanged. `PAGE_SIZE = 6` still, now at `settings.js:65` (line held steady since S1). `matchingItems()` moved to `settings.js:474` (from `:421`) but still filters on the same six fields, `provider, origin, siteUrl, courseId, courseName, principalId`; no course-code field was added.

Verify commands run for this measurement pass: `pnpm build` (all 13 packages, clean), `node scripts/ux/census.mjs` (run three times), and the ad hoc `categoriesForBinding` count above, all from the worktree root, no product file touched by this pass. `df -g /Users/Braden` reported 396 GB free throughout.

## Batch S3

Batch S3 is the remember-kind cards (WI-4.1 PROTOCOL, WI-4.2 SW, WI-4.3 and WI-4.6 RUNTIME, WI-4.4 REVIEW), the PAGES "do not ask again" card (WI-4.5), the course-discovery-fields card (WI-5.1a SW, WI-5.1b PAGES), the "Courses and access" page rebuild (WI-5.2 to WI-5.8, PAGES), the desktop Home/Settings split (WI-6.1 to WI-6.3, DESKTOP), and the "Recent changes" cards (WI-6.4a REVIEW, WI-6.4b RUNTIME). This measurement pass is read-only: I edited no product file, only this document. Measured against the worktree's current state (uncommitted changes on top of the S2 checkpoint commit `a60ece6ce`), before this batch's own checkpoint commit.

`git diff --stat a60ece6ce` touches 38 files, 4,317 insertions / 1,140 deletions, plus 4 new untracked test files. Every touched product or test file matches an S3 card's file list by name or by the glob its card states (`packages/mcp-server/test/**` for WI-6.4b covers the two new `recent-changes*.test.ts` files; "the policy set script tests" for WI-4.2 covers `bridge-copy-guard.test.mjs`, `bridge-settings-announcement.test.mjs`, `bridge-settings-contract.test.mjs`; "the discovery script tests" for WI-5.1a covers the new `extension-lifecycle-authority.test.mjs`, `moodle-browser-executor.test.mjs` and `canvas-course-discovery-fields.test.mjs`), with one exception: `scripts/ux/census.mjs` changed by 1 line (the settings-page heading assertion, `"Plan and Edit settings"` to `"Courses and access"`, to follow WI-5.2's new `h1`). That file is TOOLS-owned, not listed on any S3 card; it is a cross-lane repair of the same kind the S1 and S2 sections above record, made necessary by WI-5.2's markup change, not an edit this measurement pass made.

`df -g /Users/Braden` reported 17 GB free before `pnpm build` and before each census run (checked before every heavy command; never below the 3 GB stop threshold). Ran `pnpm build` from the worktree root: all 13 buildable packages finished `Done`, so this pass rebuilt `bridge-protocol` and `canvas-connector-mcp` (WI-4.1) and `mcp-server` (WI-4.3/4.4/4.6/6.4a/6.4b) before measuring, and the census reads this batch's built output, not stale `dist`.

Ran `node scripts/ux/census.mjs` from the worktree root, three times in a row for reproducibility (identical output each run). Output, transcribed unedited:

| Surface | Sizes | Weights | LineHt | Radii | Gaps | Paddings | Small text | Small targets | Long lines |
|---|---|---|---|---|---|---|---|---|---|
| Popup | 2 | 3 | 4 | 3 | 3 | 6 | 0 of 24 | 1 | 0 |
| Settings page | 7 | 3 | 4 | 3 | 5 | 7 | 0 of 34 | 3 | 0 |
| Setup guide | 5 | 3 | 3 | 3 | 2 | 6 | 0 of 21 | 1 | 0 |
| Edit list | 6 | 3 | 4 | 2 | 3 | 9 | 0 of 14 | 0 | 1 |
| Desktop app, set up | 5 | 3 | 5 | 3 | 5 | 9 | 0 of 25 | 2 | 1 |

Measured in the harness. Setup guide and Edit list match the Batch S2 table in this document cell for cell: neither surface's owning files (`installer/shared/setup-view.mjs`'s panel states before first course read; the review page's own CSS/markup) changed in a way the census counts. The other three surfaces moved:

- **Popup: LineHt 3 to 4, Paddings 7 to 6, small-text denominator 27 to 24.** WI-4.5 (PAGES) and WI-6.4a's popup wiring touch `popup.html`, `popup.js`, `popup-view.js` and `popup.css` this batch. I did not isolate which one line moved the denominator by 3 elements; the small-text count itself stayed 0.
- **Settings page: Sizes 6 to 7, Gaps 6 to 5, Paddings 8 to 7, small-text denominator 36 to 34, small targets 2 to 3.** Expected: `settings.js` alone changed by 1,603 lines this batch (the WI-5.2 to WI-5.8 "Courses and access" rebuild the AGENT-BRIEF's "Page structure for WI-5.2 to WI-5.5" section describes), plus `settings.html` (155 lines) and `settings.css` (182 lines). The 1-small-target increase (2 to 3) is one below WI-F.2's target of 0 and is a candidate for a later repair pass; I did not chase which control it is, since this card measures and does not edit product files.
- **Desktop app, set up: Radii 4 to 3, Gaps 6 to 5, Paddings 12 to 9, small-text denominator 50 to 25, small targets 0 to 2, Long lines 4 to 1.** Traces to WI-6.1 to WI-6.3 (DESKTOP): `installer/shared/setup-view.mjs` moved "Setup you can change" off the completed-course Home state into its own `setupManagementView` export (now shown on Settings, not Home, per D8/D9), and replaced the state's single static example prompt with `homeStatusLines()` (three fixed status rows: Assistant/Ready, Morrow Bridge/Connected, Courses/Connected, each with one action button) plus three example prompts, each now with its own "Copy" button. The halved small-text denominator (50 to 25) follows directly from removing the "Setup you can change" block (`materialsRow` plus one row per assistant) from the state the census renders. The 2 new small targets are a candidate for a later repair pass, same caveat as the Settings page row above.

## Interaction budget, this batch

Budget rows this batch changed, one line per row in the "Interaction budget" table near the top of this document, each carrying the same source mark the row itself carries. Old value is the standing fact after Batch S2 (the most recent value any earlier section recorded for that row); new value is this batch's.

- **Stop all access.** Counted from the text: **changed.** Old: select courses (per-row checkboxes, or the existing "Select this page" bulk mode), then click "Return selected courses to Plan" (S1's citations: `settings.html:146` id="return-plan", `settings.js:1233`/`:1582` at that checkpoint); a separate "Ask first in all courses" button already covered every course at once (built WI-1.4, S1), unchanged by S2 or S3. New: WI-5.4 (PAGES, this batch) adds a "Plan. Morrow asks first." button inside a single course's own detail panel (`connector/extension/settings/settings.js:1490` and `:1522`, both calling `returnToPlan([binding], { doneMessage: ... })` directly on that one binding). This is a genuine new 1-click path for the single-course case: open the row (1 click, the `course-row-name-button` at `settings.js:1183`, `aria-expanded`/`aria-controls`), then click the detail's own "Plan. Morrow asks first." button (1 click), with no checkbox or select-mode step first. The row's target is "1 click"; this batch reaches it for one course at a time (2 clicks including the open) where the prior path needed a checkbox click plus the shared Return button. The "every course, every page" and "all courses" paths (`returnPlanButton`, `askFirstAllCoursesButton`, `courseBulkPlanButton`) are unchanged in shape, only in line number (`settings.js:2033`/`:2034`/`:1891` now, from insertions above).
- **"What did Morrow change today?"** Counted from the text: **changed.** Old: "Ask the assistant. No view exists," with WI-6.4 ("Recent changes") named as Phase 6, unbuilt, in both the baseline and the S2 sections above. New: WI-6.4a and WI-6.4b are both in this batch and are built. `packages/mcp-server/src/operation-tools.ts` registers a new tool, `morrow_recent_changes` (`:298` to `:322` in the diff), whose description is "Give the person a one-time link to Morrow's recent changes page: the last 50 finished operations, each with its plain label, course, item reference, time, state, a link to its own status page, and a ready request to reverse it." It calls a new `recentChangesUrl()` on the runtime and returns the link as text plus `structuredContent`. `packages/mcp-server/src/approval-server.ts` adds the page itself: `issueRecentChangesEntry()` mints a one-time code exchanged once for a 900-second session cookie (`RECENT_ENTRY_TTL_MS`, `RECENT_SESSION_TTL_MS`, both 900,000 ms; `MAX_RECENT_ENTRIES`/`MAX_RECENT_SESSIONS` both 16), `recentChangesContent()` reads the controller's existing `operationList(200)` (F20), keeps the 50 most recently finished operations (`RECENT_OPERATIONS_READ = 200`, `RECENT_CHANGES_SHOWN = 50`), and `recentChangeRow()` renders each as its plain label, course/item context, time, state, a "See this change" link to `/operations/<id>`, and a "Copy the request" button carrying a ready-made reverse-this request the person can hand back to the assistant. `native-tool-manifest.ts` lists `morrow_recent_changes` in `fullNames` (new line, after `morrow_operation_wait`). New test files `packages/mcp-server/test/recent-changes.test.ts` and `recent-changes-tool.test.ts` (both untracked, added this batch) cover the entry/session exchange and the tool. The row's target, "1 click to a list with links to each item," is now reachable: the assistant calls the tool once, the person clicks the one link it returns, and lands on a list where each entry's own link and reverse-request are one click/one copy away. I did not attempt a live walkthrough with a real assistant session (a person check, not this agent's to claim); the mechanism itself is built and its two new test files target it directly.
- **Find one course among 40.** Measured in the harness and counted from the text: **changed, both halves.** Old, measured in the harness: `PAGE_SIZE = 6` at `settings.js:65` (S1/S2 checkpoint), so 40 courses paginated into `⌈40/6⌉ = 7` pages. Old, counted from the text: `matchingItems()` filtered on `provider, origin, siteUrl, courseId, courseName, principalId`; no course-code field existed. New, measured in the harness: `PAGE_SIZE` no longer appears anywhere in `settings.js` (`grep -c PAGE_SIZE` returns 0); `matchingItems()` no longer exists either, replaced by `renderCourseList()` (`:1277`) and a filter test inline at `:536`-`:540` that runs over the full course set with no page cut. New, counted from the text: `connector/extension/src/canvas-content.js`'s `courseSummary()` (WI-5.1a) now reads Canvas's own `course_code` field (via `trimmedCourseField(value?.course_code)`) into a `code` property, requesting `include[]=term&include[]=favorites` on the discovery call; `connector/extension/settings/settings.js:536`-`:537` (WI-5.1b/5.2) matches the search query against `` `${row.name} ${row.code}` ``, so a Canvas course code such as "BIO101" is now itself searchable, meeting the row's target ("Type the course code") directly. The 7-pages fact is gone along with `PAGE_SIZE`; the settings page now renders every matching course in one filterable, scoped list (`state.filters.q/platform/term/scope`), not a paginated one.
- **Allow a whole area of work.** Counted from the text: **changed.** Old: "Up to 141 checkboxes, one at a time," not independently re-verified past the baseline's structural check; S2 corrected the underlying catalog count to 338 grantable options (58 `requiresFieldSelection`) but added no group-level control. New: WI-5.5 (PAGES, this batch) adds `renderArea()` (`settings.js:955`) and `renderKind()` (`:924`), neither of which existed at the S2 checkpoint (confirmed: `git show a60ece6ce:.../settings.js | grep 'function renderArea'` returns nothing). Each of the 10 curated areas (`AREA_LABELS`, `:153`-`:163`: pages, assignments, quizzes, discussions, files, calendar, people, accessibility, beyond_course, other) gets one `data-area-select` checkbox that selects every non-removal option in that area at once (`:966`, "Select all in `<area>`, except removal"; removal is excluded by design, D6, and gets its own count and its own checkbox one level down). Within an open area, each of up to 3 kinds (`KIND_ORDER`: edit, publish, remove) gets its own `data-kind-select` checkbox (`:942`). `syncCustomizeTriStates()` (`:1017`) sets each box's `checked`/`indeterminate`/`aria-checked="mixed"` from `state.selectedCategories` alone, in place, matching the AGENT-BRIEF's "a checkbox change updates counts and mixed states in place and does not call `renderCustomize` again" rule. This reaches the row's target, "1 checkbox for an area," directly: a whole area's non-removal options are now one click, where before every option was its own checkbox with no group control. The underlying 338-option catalog count itself is unchanged (`src/edit-policy.js` and `connector/extension/generated/` are untouched in this batch's diff), so the fact this changes is the control surface over that catalog, not the catalog's size.

I did not find a source-file match, or found the matched file's diff outside the counted area, for the remaining budget rows in this batch's 38-file diff, so I recorded each as unchanged:

- **Setup, to the first read of a course.** Unchanged. `installer/shared/setup-view.mjs` did change this batch (WI-6.1/6.2/6.3), but only in the completed-course Home state (added `homeStatusLines()` and per-prompt Copy buttons, replacing the old single static prompt) and in moving "Setup you can change" from Home to a Settings-only `setupManagementView()` export; none of that touches the four panel states the 19/21-action walk-through counts (choose/set up an assistant, install/add the Bridge, open the course in Chrome, check the connection), which are earlier in the same file and are not in this batch's diff hunks.
- **One approved change.** Unchanged. `packages/gateway-core/src/result.ts` (F16) is untouched this batch (no diff). `packages/mcp-server/src/operation-tools.ts`'s `morrow_operation_wait` (F17, built S2) is not edited by this batch's diff to that file, which only adds the new `morrow_recent_changes` tool. `approval-server.ts` gained a `rememberButton` on the same approve form (WI-4.4), an addition alongside the existing one-click approve, not a change to the approve step itself.
- **The same kind of change again, same course, same day.** Counted from the text: **the mechanism is now built, but I did not find the offer live end to end this pass.** Old: no memory existed; S2 added only the `rememberable`/`routine` data fields (WI-3.1/3.2), with the behavior half named as Phase 4, not yet built. New: WI-4.1 (PROTOCOL) adds `merge`/`expiresInMs` to `BridgeEditPolicySet` and a fixed `BRIDGE_EDIT_DURATIONS_MS` list (30m/1h/4h/8h/24h) that `packages/bridge-protocol/src/index.ts`'s `normalizeBridgeEditPolicySet()` validates; WI-4.2 (SW) wires `merge`/`expiresInMs` through `bridgePolicySet()`/`applyBridgePolicySet()` in `service-worker.js`; WI-4.3/4.6 (RUNTIME) add `rememberOffer(operationId)` and `rememberKind(operationId)` to `runtime.ts` (both new, confirmed absent at the S2 checkpoint), which compute the bundle to offer from the operation's own tool and grant it merged into any active Edit authorization; WI-4.4 (REVIEW) renders the offer as a second submit button on the approve form ("Approve, and do not ask again for `<bundle>` in this course until `<time>`") and, on submit, calls `rememberChoice()` which calls `rememberOffer` then `rememberKind`. I traced the code path from the review page's button through `runtime.ts` to the storage write, but this pass did not run the review page live against a real operation to see the offer appear and be accepted (that is inside the harness, not a person check, so a later work item or the integrator should confirm it live); I report the mechanism as built from the code, not yet confirmed by a run I performed.
- **Ten changes of one kind.** Unchanged. `packages/mcp-server/src/server-instructions.ts` and `review-loop-guidance.ts` (the WI-2.3 guidance text S2 added) are untouched this batch (no diff); `approval-server.ts`'s one-operation-per-review shape is not touched by this batch's diff either, beyond the unrelated `rememberButton` addition noted above.
- **Come back the next day, tabs closed.** Unchanged. `bindingForCommand()` and the `openPlatformWhenNeeded` gate (S1) are not inside this batch's `service-worker.js` diff hunks, which touch only `bridgePolicySet()`/`applyBridgePolicySet()` (:2036-:2296) and the course-discovery functions (:2365-:2551, WI-5.1a's `courseMeta` plumbing).
- **"Can Morrow change anything now?"** Unchanged. `refreshBadge()` (S1/S2) is not inside this batch's `service-worker.js` diff hunks either, for the same reason.

Verify commands run for this measurement pass: `pnpm build` (all 13 packages, clean) and `node scripts/ux/census.mjs` (run three times, identical output), both from the worktree root, no product file touched by this pass. `df -g /Users/Braden` reported 17 GB free before the build and before each census run.
