# Morrow active completion goal

Status: active. Replacement goal set 6 September 2026, 15:32 UTC. The detailed controlling contract is [MORROW-1.0-COMPLETION-GOAL.md](MORROW-1.0-COMPLETION-GOAL.md).

The replacement goal retains the full unfinished provider and privacy scope. It explicitly requires a consumer desktop app and native guided installers on both Mac and Windows, plus automatic app updates and supported Bridge update coordination. All product surfaces must meet the same design standard. Independent adversarial checks and actual installed-platform proof are required. The temporary Developer mode exception applies only to Bridge until the Store listing is ready.

Current user direction: do not use Fable. The Fable review process is stopped. All remaining iPhone capture and video work is deferred until tomorrow. Preserve the authentic ChatGPT and Claude captures already saved under `launch/video/phone/capture/`; the prompts were not submitted and do not prove an LMS operation. Continue the core product, website, and main desktop film. Braden set a roughly ten-hour release deadline at 05:16 UTC on 6 September. Prepare a complete, reviewable result within that window. Do not publish or make a world-first claim without supporting evidence.

Latest direction at 14:36 UTC: remove unnecessary heading wraps across the product. Use concise fixed titles that fit at narrow widths; preserve readable type, full native course values, and layout accessibility. Add first-install Bridge onboarding with more guidance. The implementation uses optional Guide me and Quick setup paths; these change guidance only, never permissions or defaults. Setup opens once on first installation, can be reopened, resumes from current state, and confirms completion only when Morrow and a selected course are connected.

Latest direction at 14:44 UTC: Braden supplied Cloudflare access and explicitly authorized publishing the website. Add a small GitHub star link at the top and complete a systematic polish, function, privacy and consistency pass. The existing Cloudflare Pages project is `meetmorrow`, with configured custom domains `meetmorrow.app` and `www.meetmorrow.app`. Fresh DNS inspection found no apex or www record. The root owns the exact deployment and fresh live verification. The existing GitHub repository `example-owner/morrow` is private; audit its contents and history before proposing a separate visibility change. Never put the deployment credential in source, artifacts, or logs. Public website authorization does not expand supported product capabilities.

## Control and working copy

Latest direction at 15:16 UTC: build an actual guided installer for Mac and Windows. The user requires a consumer product with no commands, configuration-file editing, path typing, or developer setup in the normal Morrow app flow. The rejected command-based onboarding and MCP ZIP are historical engineering evidence only. The chosen implementation is one Electron app with shared guided screens, a macOS DMG and a Windows NSIS installer. It must detect supported assistants, preserve existing settings, bind optional materials to a chosen folder, keep private state outside signed resources, verify the connection, and lead to a first safe course preview. Run the actual installer on each platform before claiming platform support. App signing and notarization remain release work; this Mac has no valid code-signing identity, and the repository has no configured signing secrets at the current read.

Latest direction at 15:30 UTC: the Chrome Web Store account uses `hello@example.com`. The user explicitly permits a temporary Developer mode installation for the Bridge until the Store listing is finalized. This exception applies only to the browser extension. Provide visual, guided folder selection from the Morrow app; keep all other setup free of commands and technical configuration. Replace this temporary step with the real Store link when it is ready.

Current ownership: `installation_finish` owns Electron main/preload, Windows state security, updater runtime integration and native package readiness; `mcp_functionality` owns sealed package inputs, the MCP runtime manifest and final payload provenance; `launch_desktop` owns the installer renderer, Bridge onboarding and Blackboard setup UI; `canvas_repairs` owns Canvas guarded repairs and exact readback closure; `moodle_modules` owns core Moodle modules and staged package files; `moodle_learner_gradebook` owns the aggregate Assignment-submission slice; `privacy_closure` owns the Blackboard API package and MCP boundary; `program_scale_review` owns the native Windows release workflow and smoke harness; `privacy_review` independently reviews the security boundaries. Root owns integration, signing/account discovery, website deployment, source publication and final verification.

Website checkpoint 2026-09-09 (live): production deployment is `65ca95be` on `meetmorrow.app` and `www.meetmorrow.app`, verified by live readback (rewritten conversations, dictated hero bullet, provider qualifiers). Previous checkpoint at 18:15 UTC: deployment `ad68fdac-d404-4938-a1d8-845de52a3868`. The plain-language rewrite covers all 17 pages and passed checks at five widths. All 35 public files match the frozen stage on the deployment URL and both custom domains. Root inspected the live homepage. The conversations play once per tab and stay visible. See `output/cloudflare-website-plain-language-20260906-1815/public-readback.json`. `hello@meetmorrow.app` still forwards to `hello@example.com`; its rule and DNS were verified, but no delivery test has been sent.

Latest direction at 17:57 UTC: Braden authorized his existing signed-in Chrome profile to test BT2. Fresh course settings show `89585`, now named `BIOL 101: General Biology`, code `BIOL 101`. Use disposable test content for authorized write checks; do not infer authorization to send real student messages or change real grades. Browser automation policy blocks opening the installed extension page, so that setup step needs a person in Chrome. Do not work around that block.

All implementation and agent coordination run in Codex thread `01a074d1-064d-78a2-b4b9-ee222073fcd4`. The earlier `Morrow Federation` thread, `01a071ed-2194-7592-8e8e-696f2e899997`, is a read-only source of history. Its controller confirmed that all nine old workers stopped. Do not resume it.

Working copy: `/Users/example/Projects/.morrow-worktrees/morrow-1.0-final-20260907`, branch `codex/morrow-operation-completion-20260907`. Recovery started at commit `6c15a421a519ea94fd868aef1b3bb808a995f145` with substantial unfinished changes. Preserve those changes, existing evidence, and transcripts.

The active goal is to complete Morrow as a reliable LMS operations layer, with all requested functionality and Apple-level polish across the MCP, Chrome bridge, settings, review and result screens, installation, website, brand, and video. This includes the full scope in [MORROW-REMAINING-WORK.md](MORROW-REMAINING-WORK.md) and [MOODLE-FULL-FUNCTIONALITY.md](MOODLE-FULL-FUNCTIONALITY.md). A smaller completed feature must not silently replace that scope.

## Reuse the proven system

Braden requires the proven parts of the legacy Morrow extension and `example-lms-vps` to inform this implementation. Start from actual code and tests. Adapt the exact course/account-scoped roster, stable student-token swap, recursive outbound filtering, authority checks, one-send execution, saved-state verification, and useful course design/audit knowledge. Record source provenance and verify the adapted behavior at the new boundary. Do not replace a proven control with an incomplete generic substitute.

ExamplePlatform inspection is SSH-only. Legacy Morrow is a reference implementation; do not change it while integrating the new product. Do not clone donor code. Write original implementations that retain the proven contracts and improve their behavior, scope, recovery, or verification. Record each concrete improvement and its tests.

## Observable completion criteria

1. **Student privacy.** Automatically replace student identities before any result reaches an assistant. Bind roster discovery and tokens to the exact site, signed-in account, course, session, and privacy context. Cover nested learner records, known names in text/HTML, identifiers, resources, reports, operation projections, errors, and model sampling. Keep private verification bodies and identity maps local. Test incomplete rosters, ambiguous names, Unicode, restart and concurrent scopes. Explain the actual protection and residual limits; do not claim universal PII detection or that all course data stays local.
2. **Program-wide accessibility, design and curriculum work.** Discover and audit every readable selected-course surface. Include Pages, Assignments, Discussions, Files, Classic Quizzes, New Quiz instructions/questions/choices/answers/feedback/stimulus/media, Item Banks and linked materials. Persist each target and its evidence. Build guarded remediation by surface, preserve course design and unrelated settings, dispatch once, reread saved content and recheck it. Preserve unread, inaccessible, truncated, manual, unsupported and uncertain states. No empty-result accessibility pass.
3. **Moodle and Blackboard.** Complete the Moodle core modules, question types, file lifecycle, learner and grading functions, dates, completion, restrictions, reports, course lifecycle and administration in the full contract. Implement Blackboard through the official Anthology REST API and mocked HTTP contract tests. Mark live tenant behavior untested; Braden has no test course and does not require one for release. A catalog entry or HTTP success is not implemented functionality. Optional site plugins and restricted providers need their own supported contract.
4. **Dozens of courses and multiple assistants.** Discover selected courses through shared site sessions without one tab per course. Preserve separate account/course bindings. Support bounded concurrency, ownership, conflict refusal, cancellation, partial progress, restart, retained reports and exact child results. Never automatically replay an uncertain write.
5. **Plan/Edit and settings.** Deliver understandable Plan and Edit controls, explicit edit-category checkboxes, course scope, clear expiry/revocation, useful defaults and all connection/error states. Conversational grants require the person's exact signed confirmation and bridge readback. Generic assistant approval cannot grant Morrow authority. Test due-date-only scope, unsupported changes, stale binding, decline/cancel, and uncertain outcomes. Verify real phone behavior before selling it as supported.
6. **Two installations.** Package everything needed in the Morrow MCP and Chrome bridge. Users must not need a source checkout, Node, pnpm, a token file, or a third Morrow service. Provide simple instructions and prove startup, project-scoped client setup, clean shutdown and supported platform behavior from the actual extracted package.
7. **A complete visual product.** Use the transparent knot, Manrope, cobalt, cool white and chartreuse consistently. The hero must make the benefit clear through five meaningful scenarios led by program-wide accessibility audit and remediation, including New Quizzes and Item Banks. Other scenarios cover course creation, changes across sections, curriculum review and phone work. Use authentic Canvas captures in balanced proof sections and accurate current ChatGPT/Claude Remote visuals in licensed device frames. Inspect responsive screenshots, motion, reduced motion, keyboard use, contrast, long names and dense course lists. Keep copy purposeful, factual, clear and lightly cheeky. Avoid awkward wrapping, empty micro-labels, cursor artifacts and generic AI slogans.
8. **Evidence and delivery.** Reconcile source, documentation, claims, assets and package receipts. Run focused regression tests and the actual repository broad gate (`pnpm check` in this working copy), then relevant browser and package checks. Inspect after screenshots. Verify live writes on an authorized exact disposable target with authoritative readback and replay refusal. Report remaining limits precisely. Do not treat local checks or an earlier ZIP as full product or release proof.

## Current agent ownership

All workers are native children of the current thread. The root owns integration, exact live proof, shared MCP entry points, the work ledger, and final gates.

| Agent | Current work | Primary ownership |
| --- | --- | --- |
| `privacy_closure` | Canvas bulk-date/enrollment postconditions complete | Exact raw-date validation, Progress/date and enrollment subject/state verification |
| `canvas_repairs` | Private Canvas Inbox pipeline source-frozen | Opaque public planner, private sealed dispatch and actual Full MCP client fixture |
| `mcp_functionality` | File transfer and Gradebook schema complete | Authenticated Canvas confirmation, external cookie confinement, signed-URL collision refusal |
| `moodle_modules` | Forum/group read contracts complete | Native bounded transport, participant privacy and unknown-author refusal |
| `privacy_review` | Independent Inbox/file-boundary review active | Read-only source and test review |
| `website_finish` | All 17 pages verified and frozen | 85 viewport checks, one-time conversation behavior and publish manifest |
| `program_scale_review` | Native Windows CI support active | Private branch/run and retained install diagnostics |
| `installation_finish` | Windows child startup root cause active | Installed gateway exits before initialize; final Mac build waits the broad gate |
| `launch_desktop` | Role-page copy complete | Root second pass and final website checks integrated |

The earlier controller and its nine workers remain stopped. Do not resume them. Serialize shared-file changes and retain narrow ownership. Do not freeze packages while source writers are active.

## Recovery facts and next integration work

- The old privacy audit found structured student identities and free text could reach the client. Lesson review sampling and native outputs lacked a common final privacy boundary. No accepted PII fix existed at recovery.
- Conversational multi-course Edit/Plan and encrypted retained native audit results have source and focused-test evidence. Recheck the current files and fix real gaps. Do not replace the current multi-course API merely to match an older single-course plan.
- Due-date-only Edit classification admitted an empty changed-field set. The executor refused it before send, but the permission classification still needs correction. The Settings fallback also lacks the Canvas due-date category.
- Full inventory, file remediation, Blackboard transport, complete Moodle and actual remote phone proof remain substantial work.
- The latest phone still and privacy screenshots had not received root visual review. The five-scenario hero was not implemented at recovery.
- The last verified private MCP archive predates URL, conversational Edit, due-date, native batch and PII work. Its digest is `3b540188cde390a482572da10d1ac412de711a5a2960d5b3396c37f6120bc415`; it is historical package evidence only.

## Earlier implementation evidence

These records describe their dated source or artifact. The current checkpoint is in [MORROW-REMAINING-WORK.md](MORROW-REMAINING-WORK.md).

- A quiet `pnpm check` passed for the 14:03:47 UTC archive, with 51 passing script checks and one intentionally skipped package smoke. Log: `output/morrow-check-2026-09-06-privacy-final.log`. That exact archive then passed the explicit extracted-package smoke and the separate iMac recipient verifier. The later historical-inspection, gradebook-review and status-copy corrections are active source work and require another gate and build.
- Canvas inventory includes Pages, Assignments, Discussions, Files, Classic and New Quizzes, nested question evidence, and readable Item Bank entries. Course-scoped File metadata now proves the selected course association. Shared-bank dependencies and actual file bytes remain separate contracts.
- Page, Assignment, Discussion and direct New Quiz QuestionItem image-alt repairs have protected native-browser fixtures. New Quiz choice, answer-feedback and question-feedback guards also have planner, browser and exact Edit-authority checks. Classic questions and Stimuli remain held: the source review did not establish a complete preserved native writer or shared-bank scope.
- Exact-course conversational Edit/Plan grants have signed native confirmation, expiry, revocation and current-state readback. Expanded settings list individual catalog actions, with on-demand details so large course sets do not duplicate the full rule catalog. The root MCP fetches the selected binding's exact rule details and refuses Review-only actions. Phone grants remain unverified and deferred.
- File staging and dispatch are implemented for one Moodle Resource. At 07:45 UTC on 6 September, a standard Teacher in a separate hidden public-sandbox course completed one reviewed upload and Resource save. The gateway reached `verified` after one dispatch; an independent native read matched the hidden state and all 141 saved bytes by SHA-256. Replay was refused without another POST. The [dated receipt](../../output/live-moodle/resource-file-2026-09-06/README.md) records the exact target and frozen-overlay limits. Earlier unresolved operations remain unchanged and locked. This does not prove replacements, folders, larger files or a final archive.
- Moodle now has additional native settings and creation layers for Book, Lesson, Glossary, Wiki, Feedback and Database. The public catalog is changing as question writes are held. Creating a new ready Question Bank entry can change future random selections in other Quizzes; updating an existing entry can also change latest-version references. Core native views do not provide a complete reference manifest or exact-slot clone replacement. Question authoring stays held where that scope cannot be proven. Exact reads remain useful. The full Moodle contract remains open.
- Canvas privacy covers known structured/free-text, multi-scope, sampling, report, result-artifact and operation-summary paths. Result handles bind to the MCP client/session. Retained program reports are redacted before encrypted storage. Moodle's private roster has actual administrator and standard-teacher read evidence and the final teacher file operation passed the public MCP privacy boundary. No universal PII claim is established.
- Selected-program inventory reuses durable read-only batches and idempotent audit conversion. An actual runtime/connector fixture with three MCP clients passed 40 mixed Canvas/Moodle audits, separate accounts/rosters, bounded concurrency, retained/egress privacy, cancellation, restart and no uncertain retry. Moodle inventory now converts exact module introductions, separate Book chapters and the 12 existing readable question types, with explicit gaps for nested content and files. This remains fixture evidence. A separate review found and corrected shared-object locking across different principals and connector sources; persisted older-lock compatibility passed independent review. Potentially sent legacy records with unknown target scope hold all new connector dispatches. A record without a retained standalone read comparator remains unconfirmed after restart; no safe general provider-evidence recovery path is implemented yet.
- Canvas text/HTML file reading passed actual native Chrome Allow, Deny and Remove decisions in a fresh isolated profile. The real cross-origin synthetic file read sent no storage cookie, refused a different course and removed the learner name before gateway egress. The repeated test also passed with a UTF-8 byte-order mark. The [final safe receipt](../../output/canvas-file-permission-2026-09-06/native-permission-final-receipt.json) and [source inventory](../../output/canvas-file-permission-2026-09-06/native-permission-final-source-inventory.json) bind the tested manifest and source. The final native run passed the exact-event permission guard. First-grant connection opened course selection after one click. Those artifacts predate the static `webRequest` permission for reviewed Canvas uploads and do not prove the current manifest or upload response observer. These are synthetic-host proofs, not a customer Canvas host or final archive. PDF, Office and unknown binary content remain explicit manual/unread cases.
- The bounded gradebook layer implements configuration reads and category/manual-item rename. Native Moodle 5.2.2 uses menu triggers, not the old edit links. The parser now requires a complete native tree and binds each menu control to the category or item identity in that row. Independent review and focused regressions pass. A current standard-Teacher read finds the root category and two non-manual Quiz items in the sample course; the earlier zero-count receipt was false and does not prove an empty gradebook. A separate hidden course 5 now contains one hidden manual grade item for the final-package write check. No live gradebook write is established yet.
- Activity moves now include Label and URL on verified core topics/weeks course formats. Initial and pre-send format checks refuse custom formats; the final native format read is part of verification. Eight focused browser tests and independent review pass, including no-send preflight refusal and an unconfirmed result after a lost final format read. Native Label/URL move proof remains pending.
- A browser transport reproduction found two applied HTTP/1.1 POSTs from one explicit Chromium fetch after a connection loss. Morrow performs one explicit dispatch and does not retry uncertain writes; it cannot guarantee exactly-once provider effects without provider deduplication. The [transport limit](BROWSER-POST-TRANSPORT-LIMIT.md) records the observed browser behavior and official source. Preserve uncertain operation locks and do not retry them automatically.
- Blackboard is moving to the official Anthology REST API per Braden's latest direction. The API package and MCP integration are in progress; live tenant behavior must remain explicitly untested. No test course or browser PKCE exchange is a release gate. Connect and MindTap remain excluded under the recorded source-rights review.
- App-owned unpacked Bridge updates now have a coded maintenance contract. It verifies the release tree and fixed extension identity, proves the active folder through a challenge, fences pending and uncertain writes, swaps atomically, retains rollback, and requires exact paired-Bridge readback before release. The Developer mode flow still requires a person to reload the extension in Chrome. Chrome Web Store updates remain Chrome-owned.
- Braden explicitly requires the desktop app to update the MCP runtime. The signed app artifact already carries `MorrowPayload`; current work is adding an exact MCP package/version/hash manifest plus post-restart gateway readback, while preserving the stable assistant path and all private state.
- The generated client entries now use each canonical project directory, with absolute bundle server paths. Each authenticated owner session binds one private immutable workspace root. The 13:30 UTC archive passed two installed projects with distinct same-named file SHA-256 values, cross-project and symlink refusal, no workspace-path or byte leakage, and one shared owner. It also passed a separate iMac recipient check under a restricted environment with no global Node/pnpm, four actual SDK client health calls, and normal owner shutdown. The bridge was unpaired on the iMac. The [archive receipt](../../output/private-mcp-candidate-2026-09-06T13-27-55-317Z/morrow-v1.0.0-rc.0-darwin-arm64-mcp.receipt.json) and adjacent iMac receipt are historical proof for ZIP SHA-256 `e7e8cdb85d8b388396107e6d7c9150209631f820eaae51357f58fa0caed9f2d4`; they do not prove later source.
- Actual native testing of that archive found three further defects before any provider write. Public binding discovery failed under the generated learner-privacy default. Unchecked native gradebook date widgets put the current minute in snapshot digests. General learner redaction decoded safe approval URLs from `op%3A` to `op:`, which correctly failed the native approval cookie/referer check. The two file plans and affected move plan were cancelled with zero dispatch attempts. The typed binding projection and exact inactive-widget digest correction now have focused tests; the redactor must preserve every unmatched original span and retain encoded-secret detection. Independent review is active. A move-review correction also makes identically named sections clear by showing their section numbers and a plain source section. Build a new archive only after these corrections pass review and the broad gate.
- The 14:03:47 UTC unmodified archive has SHA-256 `4d545da0f9a5a9d43873f93d391eaf5525223892d76af1dd07fea8c9bdf8aab6`. It passed native Chrome permission/pairing and public MCP course discovery under the generated learner-privacy default. Two actual installed client configurations shared one owner while preserving distinct workspace file digests and refusing traversal/symlink escapes. In hidden course 5, a Label and URL each moved from section 26 to 27 with one observed move POST, native saved-section readback, and replay refusal. One hidden Resource (module 12) saved after one upload and one form-save POST; a separate native download matched all 141 bytes by SHA-256. Gradebook inactive-date digests stayed identical across a minute change. The [installed evidence](../../output/live-moodle/installed-final-bundle-2026-09-06/receipt.json) binds these results to that ZIP.
- Native testing exposed two more user-flow gaps. Grade-item plans have no matching review context, so the UI correctly withholds Apply. Both grade plans were cancelled with zero dispatch attempts; no live grade rename is established. After the test harness stopped, normal course reconnection created a new session binding. Historical operation inspection then failed its old learner context, including a cancellation response whose actual state was already cancelled. Read-only journal snapshots confirmed the unsent state; no live database was changed. The active fix returns only a validated operation ID, state, dispatch count, verification status and fixed omission reason when the historical learner context is unavailable. It must not rebind old content to a new roster or change replay locks.
- The five-scenario website and expanded settings have current browser screenshots and responsive checks. The main 42-second film uses authentic product captures and clearly marked examples. Root inspected the revised closing knot and controls crop. The final film and poster are synced to the website with matching hashes. All remaining phone work is deferred.

Next integration order: finish the Windows child-startup root cause and independent Inbox/file review. The root is running a quiet current `pnpm check` in `output/morrow-check-20260906-1820.log`. After it passes, build fresh native candidates and verify the packaged assistant/Bridge/course first-read path, native repair/uninstall and signed update/recovery. Complete the remaining Canvas, Moodle, Blackboard and scale contracts; do not turn held or untested operations into finished features. Recreate expired disposable Moodle fixtures before any new proof. Keep phone capture deferred.

The active goal remains open until the required work and material verification are complete. Preserve unrelated changes. Website publication is authorized. Source visibility, outreach, unrelated live production changes and irreversible operations still require the needed authority. Do not start Docker or automate system Chrome.

Latest Blackboard direction: implement API support and state that it needs live testing. Do not ask Braden for a Blackboard test course again. Local credentials stay outside assistant inputs and outputs.
